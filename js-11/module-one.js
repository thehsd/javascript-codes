// variables
// functions
// classes => export

// export const name = "alireza";

export const userName = "hsddev";

export const printName = (name) => {
  console.log(" name : ", name);
};

export const createTag = (tagname) => {
  const tag = document.createElement(tagname);
  return tag;
};
